yum repolist all # List all yum repository
yum-config-manager --enable/disable puppetlabs\* # Disable all yum repository whose id start with puppetlabs 
yum-config-manager --add-repo repo_url # Add repository to repostory list
